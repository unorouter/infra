apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Shared DB plumbing (ClusterSecretStore + pg-s3 ExternalSecrets) and the monthly restore drill. The CNPG
# Cluster/ObjectStore/ScheduledBackup manifests live in the app repos (new-api,
# unorouter-bot k8s/pg.yaml), deployed via the services ApplicationSet.
resources:
  - eso-pg-s3.yaml
  - dr-drill.yaml
