apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# top-level: aggregate the per-DB kustomizations so ArgoCD renders one root (not recurse,
# which double-counts the child kustomization.yaml files).
resources:
  - newapi-pg
  - bot-pg
  - redis
